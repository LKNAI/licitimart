# Copie para .env.local e preencha com um projeto Supabase real.
# Nenhum destes valores existe configurado neste ambiente ainda --
# ver src/licitimart/web/plan.md ("falta") e ERS v1 secao 2.3.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Chave de LLM -- nao disponivel neste ambiente (ver CLAUDE.md do
# projeto). Sem ela, telas que dependeriam de citacao validada por IA
# (RNF-010/011) ficam com dado mock, nunca fingindo resposta real.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
