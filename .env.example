# Copie para .env (raiz do projeto, ja gitignorado) e preencha com o
# projeto Supabase real. Usado pelo lado Python (scripts/exportar_para_supabase.py)
# -- nao confundir com src/licitimart/web/.env.local, que e o lado Next.js
# e usa a chave anon (publica), nunca a service_role.

SUPABASE_URL=
# service_role -- ignora TODA RLS. Nunca no chat, nunca commitada, nunca
# usada no lado browser/Next.js.
SUPABASE_SERVICE_ROLE_KEY=
